# IGDB (Twitch) — primary game metadata; server-only (Bearer via client credentials)
# https://api-docs.igdb.com/
IGDB_CLIENT_ID=
IGDB_CLIENT_SECRET=

# IsThereAnyDeal — prices, deals, store links; server-only
# https://docs.isthereanydeal.com/
ITAD_API_KEY=

# Supabase — create project at https://supabase.com/dashboard
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only (never expose to client)
SUPABASE_SERVICE_ROLE_KEY=
